apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingest-scheduler
  namespace: smap
  labels:
    app.kubernetes.io/name: ingest-scheduler
    app.kubernetes.io/part-of: smap
    app.kubernetes.io/component: scheduler
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: ingest-scheduler
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ingest-scheduler
        app.kubernetes.io/name: ingest-scheduler
        app.kubernetes.io/part-of: smap
    spec:
      imagePullSecrets:
        - name: zot-registry
      containers:
        - name: ingest-scheduler
          image: 172.16.21.10:5000/smap/ingest-scheduler:latest
          imagePullPolicy: Always
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          envFrom:
            - configMapRef:
                name: ingest-config
            - secretRef:
                name: ingest-secrets
          env:
            - name: CONTAINER_NAME
              value: "ingest-scheduler"
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
      terminationGracePeriodSeconds: 30
      restartPolicy: Always
